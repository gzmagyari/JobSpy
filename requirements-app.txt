# Dashboard app dependencies (in addition to the jobspy library deps installed via `pip install -e .`)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlmodel>=0.0.22
openai>=1.50.0
apscheduler>=3.10.0
python-dotenv>=1.0.0
